name: Test linux job
on:
  push

jobs:
  testJob:
    name: Test
    runs-on: ubuntu-latest
    outputs:
      packageJson: ${{ steps.set_var.outputs.packageJson }}
    steps:
      - uses: actions/checkout@v2
      - id: set_var
        run: |
          content=`cat package.json`
          # the following lines are only required for multi line json
          content="${content//'%'/'%25'}"
          content="${content//$'\n'/'%0A'}"
          content="${content//$'\r'/'%0D'}"
          # end of optional handling for multi line json
          echo "::set-output name=packageJson::$content"
      - run: |
          echo "${{fromJson(steps.set_var.outputs.packageJson).version}}"
          echo "${{fromJson(steps.set_var.outputs.packageJson).devDependencies}}"
  testJob2:
    needs: testJob
    runs-on: ubuntu-latest
    steps:
      - name: Dump Github Context
        run: echo '${{ toJSON(github) }}'
      - name: Print package Json content
        run: |
          echo "${{ fromJson(needs.testJob.outputs.packageJson).devDependencies.jest}}"
          echo "${{ fromJson(needs.testJob.outputs.packageJson).devDependencies['@fattmerchantorg/types-engine']}}"
      - name: Check types value
        if: ${{ fromJson(needs.testJob.outputs.packageJson).devDependencies['@fattmerchantorg/types-engine'] == 'Hello' }}
        run: echo "greeting is Hello"
      - name: Success
        if: ${{ success() }}
        run: echo "Still running..."
      - name: Always
        if: ${{ always() }}
        run: echo "You will always see this"
      - name: Failure
        if: ${{ failure() }}
        run: echo "Something went wrong..."
      - name: Terminate Output
        run: echo "Ending Now....."
